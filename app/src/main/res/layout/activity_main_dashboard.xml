<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout 
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F9FAFB">


    <!-- Banner de estado de conexión -->
    <TextView
        android:id="@+id/tvConnectionBanner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🔴  Sin conexión — Modo local activo"
        android:textColor="#FFFFFF"
        android:textSize="13sp"
        android:gravity="center"
        android:padding="10dp"
        android:background="#DC2626"
        android:visibility="gone"
        android:elevation="8dp"
        android:translationZ="8dp" />

    <!-- NavHost Fragment Container -->
    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/nav_host_fragment"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="60dp"
        app:defaultNavHost="true" />

    <!-- Bottom Bar wrapper for bottomNav and FAB -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottomAppBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:backgroundTint="#FFFFFF"
        app:fabAlignmentMode="end"
        app:fabCradleMargin="8dp"
        app:fabCradleRoundedCornerRadius="16dp"
        app:fabCradleVerticalOffset="0dp">

        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottomNav"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginEnd="80dp"
            android:background="@android:color/transparent"
            app:elevation="0dp"
            app:itemIconTint="@drawable/selector_nav_icon"
            app:itemTextColor="@drawable/selector_nav_text"
            app:menu="@menu/bottom_menu_residente" />
    </com.google.android.material.bottomappbar.BottomAppBar>

    <!-- FAB for Panic Button -->
    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabPanico"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="Botón de Pánico"
        android:src="@drawable/ic_panico"
        app:backgroundTint="#EF4444"
        app:tint="#FFFFFF"
        app:layout_anchor="@id/bottomAppBar"
        app:maxImageSize="32dp" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>
